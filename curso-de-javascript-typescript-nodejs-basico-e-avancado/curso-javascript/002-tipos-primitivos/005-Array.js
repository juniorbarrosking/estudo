/* Tipos primitivos
Array

Array = conjunto de dados, uma lista de dados
*/
console.log(["teste", 1, true]);
console.log(["teste", 1, true].length);
["teste", 1, true].forEach((res, index) => {
    console.log(res, index);
})

